<form method="post" action="{{ path('calendar_delete', {'id': users.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet intervenant ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ users.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>